# Wojak Obstacle Run

## Run locally

Use a static file server so ES module imports load correctly:

```bash
python -m http.server 8000
```

Then open <http://127.0.0.1:8000> in your browser.

## Controls

- **Space**: Jump
- **Jump** button: Jump
- **Restart** / **Play again**: Reset the run
